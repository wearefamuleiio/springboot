spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hahaxiaowei/config-repo.git
          search-paths: config-repo
      label: dev
      profile: dev
      discovery:
        enabled: true